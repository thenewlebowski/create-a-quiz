import React from 'react'

export default function Question(props) {
    // count = 4;
    // randomPosition = Math.floor(Math.random() * count) + 1
    return (
        <div>
            <h3>{props.question}</h3>
            
        </div>
    )
}
